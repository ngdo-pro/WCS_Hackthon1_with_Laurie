{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} Votez !  !{% endblock %}
{% block body %}

    <section class="container-fluid">
        <header></header>

        <!-- H1 & H2 -->
        <div class="container-fluid">
            <div class="row titles-section">
                <div class="col-md-12">
                    <h1 class="text-center">Votez pour les plus belles chaussettes de la soirée !</h1>
                    <h2 class="text-center">Cliquez sur le bouton ci-dessous pour enregistrer votre vote en renseignant vos nom et prénom.<br/>Vous pouvez choisir jusqu'à 3 paires par personne.</h2>
                </div>
            </div>
        </div>

        <!-- Button to vote -->
        <div class="container">
            <div class="row button_vote">
                <div class="col-md-12">
                    <center><a class="btn btn-vote btn-warning" data-toggle="modal" data-target="#myModal" href="#" role="button">Votez ici ! </a></center>
                </div>
            </div>
        </div>

        <!-- Modal -->
        <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                        <p class="text-center">Merci de renseigner vos nom et prénom pour pouvoir voter.<br/>Ces informations ne seront ni utilisées à des fins commerciales ni cédées à des tiers.</p>
                        <form>
                            <div class="form-group">
                                <input type="text" class="form-control" id="firstname" placeholder="Votre prénom">
                            </div>
                            <div class="form-group">
                                <input type="text" class="form-control" id="lastname" placeholder="Votre nom">
                            </div>
                            <button type="submit" class="btn btn-default">Accéder</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <!-- Cards with photos -->
        <div class="container-fluid">
            <div class="row" id="list-cards">
            {% for sock in socks %}
                     <div class="col-md-3 col-xs-12 col-sm-4">
                    <img src="{{ asset('uploads/photos/' ~ sock.imgUrl) }}" class="img-responsive img-rounded" width="200px", height="200px"/>
                    <div class="row elements-photos">
                        <div class="col-xs-6">
                            <p class="text-center">{{ sock.sockVoteCount }}</p>
                        </div>
                        <div class="col-xs-6">
                            <button type="button" class="btn btn-default" aria-label="Center Align">
                                <span class="glyphicon glyphicon glyphicon-heart glyphicon-center-align" aria-hidden="true"></span>
                            </button>
                        </div>
                    </div>
                </div>
            {% endfor %}
            </div>
        </div>

    </div>

{% endblock %}

